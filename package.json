{
  "name": "beat-dancer",
  "version": "2.0.0",
  "description": "Browser-based music rhythm dance simulator",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo build skipped - committed dist/ is the bundle",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.128.0"
  },
  "devDependencies": {
    "@types/three": "^0.128.0",
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}